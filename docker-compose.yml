services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8081:8080"
    env_file:
      - backend/.env
    networks:
      - qr-net

networks:
  qr-net:
    enable_ipv6: true
